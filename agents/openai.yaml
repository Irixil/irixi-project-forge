interface:
  display_name: "Irixi Project Forge"
  short_description: "Guide ideas to usable products across AI platforms"
  default_prompt: "Use $dz in plain, concise language. Detect this host's real capabilities, then guide my idea or current project to one clear next step."

policy:
  allow_implicit_invocation: false
