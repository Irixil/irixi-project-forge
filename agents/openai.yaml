interface:
  display_name: "Irixi Project Forge"
  short_description: "Start or resume products with verified gates"
  default_prompt: "Use $dz to start or take over this product task, reconstruct the current state, challenge missing assumptions, and continue from the earliest unsupported AI-native SDLC gate."

policy:
  allow_implicit_invocation: false
