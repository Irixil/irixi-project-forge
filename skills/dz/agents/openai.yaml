interface:
  display_name: "DZ — Irixi Project Forge"
  short_description: "Build or resume products in plain language"
  default_prompt: "Use $dz to explain the next product step in plain language, ask one question, and keep the project state current."

policy:
  allow_implicit_invocation: false
