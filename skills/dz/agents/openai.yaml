interface:
  display_name: "DZ — Irixi Project Forge"
  short_description: "Build or resume products in plain language"
  default_prompt: "Use $dz to guide my new idea, or reconcile my current project's decisions, changes, unresolved problems, and latest state, then discuss the next action with me in plain language before acting."

policy:
  allow_implicit_invocation: true
